<a data-id="{{ id }}"{{ name is defined ? ' data-name="' ~ name|replace({'"': ''}) ~ '"' : '' }}{{ remove is defined ? ' data-remove="' ~ remove ~ '"' : '' }} class="btn btn-suivre {{ class }}"><i class="glyphicon glyphicon-plus"></i> {{ text is defined ? text : 'Suivre' }}</a>