<a data-id="{{ id }}"{{ name is defined ? ' data-name="'~ name ~'"' : '' }}{{ remove is defined ? ' data-remove="'~ remove ~'"' : '' }} class="btn btn-suivre suivre-bouton-gros {{ class }}">+ {{ text is defined ? text : 'Suivre' }}</a>